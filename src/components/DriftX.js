import React from 'react';
import Drift from "react-driftjs"

const DriftX = (props) => {
    return (
      <Drift
        appId="wchwkh7fnsz3"
        userId = "dawnmaxsuppport"
        />
    )
}

export default DriftX;